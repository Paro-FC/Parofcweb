import { AboutPage } from "@/components/AboutPage";

export const metadata = {
  title: "About | Paro FC",
  description: "Learn about Paro FC — our history, values, and vision.",
};

export default function Page() {
  return <AboutPage />;
}
